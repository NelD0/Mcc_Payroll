<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all requests into the public directory where index.php lives
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L,NC]
</IfModule>
